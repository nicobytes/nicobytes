---
import clsx from 'clsx';

interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...rest } = Astro.props;
---

<div class={clsx('sm:px-8', className)} {...rest}>
  <div class="mx-auto w-full max-w-7xl lg:px-8">
    <slot />
  </div>
</div>
